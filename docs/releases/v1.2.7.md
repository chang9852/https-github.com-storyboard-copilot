## 新增

- AI 生成全链路日志：在 createGenerationTask 每个关键步骤增加 console.info/console.error 日志，便于排查"生成失败"问题
- 轮询失败自动重试：getGenerateImageJob 调用失败时不再直接抛错，而是等待后重试

## 修复

- 错误信息详情透传：节点 catch 块现在优先显示 err.details（Rust 侧错误详情），避免只显示泛化文案