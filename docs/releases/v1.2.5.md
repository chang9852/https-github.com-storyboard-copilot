## 修复

- 修复 reqwest 缺少 `rustls-tls` 特性导致所有 AI 供应商 HTTPS 请求直接失败的问题
- 修复 AI provider HTTP 客户端无超时配置，长时间生成请求可能永久挂起的问题（新增 15s 连接超时 + 300s 总超时）
- 修复应用启动时 Zustand persist 未恢复完成就调用生图接口，导致传入空 API key 触发 401 的问题